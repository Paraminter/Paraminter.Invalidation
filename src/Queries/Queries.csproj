<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <AssemblyName>Paraminter.Processors.Invalidation.Queries</AssemblyName>

        <RootNamespace>Paraminter.Processors.Invalidation.Queries</RootNamespace>
        
        <PackageId>Paraminter.Processors.Invalidation.Queries</PackageId>
        <Description>Defines queries for Paraminter processor-invalidation.</Description>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="Paraminter.Cqs.Queries" />
    </ItemGroup>

</Project>
